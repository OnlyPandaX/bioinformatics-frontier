# Multi-Omics Daily Briefing Skill

> v1.7.0 | 每日多组学研究简报自动生成

搜索 Nature、ArXiv、bioRxiv、medRxiv **最近48小时**发表的相关论文，精选 3-5 篇高价值研究，生成结构化简报并推送到 GitHub。

---

## 核心流程

```
06:30 定时触发 → 搜索当天论文 → 关键词筛选 → 去重 → 生成简报 → 推送 GitHub → QQ 通知
                                  ↓
                            无相关论文 → 发送跳过通知
```

### 搜索策略（v1.6.0）

**搜索最近48小时内发表的相关论文**，覆盖4大数据源：

1. **Nature系列** — nature.com RSS/搜索
2. **ArXiv q-bio** — arxiv.org API（分类 q-bio.GN/q-bio.QM/q-bio.CB）
3. **bioRxiv** — api.biorxiv.org（生物预印本，与ArXiv不同平台！）
   - API: `https://api.biorxiv.org/details/biorxiv/{开始日期}/{结束日期}`
   - 重点category: genomics, bioinformatics, computational_biology, microbiology, immunology, neuroscience
4. **medRxiv** — api.biorxiv.org（医学预印本，同一API不同端点）
   - API: `https://api.biorxiv.org/details/medrxiv/{开始日期}/{结束日期}`
   - 重点category: genomics, bioinformatics, oncology, infectious_diseases

⚠️ **bioRxiv ≠ ArXiv**！它们是完全不同的平台，API不通用，需要分别搜索。

用关键词筛选后，去重过滤。
**48小时内无相关新论文 → 不发简报**，用 message 工具发送跳过通知：「今日多组学简报：近48小时无新相关论文，跳过。」

### 关键词筛选

覆盖领域及对应关键词：

| 领域 | 关键词 |
|------|--------|
| 多组学 | multi-omics, integrative analysis, pan-cancer |
| 生物信息学 | bioinformatics, computational biology, pipeline |
| 基因组学 | genomics, GWAS, variant calling, epigenomics |
| 转录组学 | transcriptomics, scRNA-seq, spatial transcriptomics |
| 蛋白质组学 | proteomics, mass spectrometry, interactome |
| AI for Biology | deep learning, transformer, foundation model, AI |
| 单细胞组学 | single-cell, scATAC-seq, cell atlas |
| 微生物组 | microbiome, metagenomics, metabolomics |

### 去重机制

维护 `sent-papers.json` 已发送论文列表，避免重复推送。

- **每篇文章原则上只发送一次**
- 搜索范围仅当天后，去重主要防止跨源重复（同一论文在 Nature 和 ArXiv 同时出现）
- **自动清理**：每次更新时，自动删除超过 **30 天**的记录（搜索仅当天，超期论文不会再次出现）
- **重新发送需正当理由**：

| 理由 | 示例 |
|------|------|
| 后续进展 | 「因被 Nature 亮点报道有新进展」 |
| 被引用验证 | 「该研究被另一篇 Cell 文章引用和验证」 |
| 趋势汇总 | 「该方向连续发表 3 篇重要论文，值得汇总评述」 |
| 新数据集 | 「作者团队发布了配套大规模数据集」 |

去重列表路径：`sent-papers.json`（本 skill 目录下）

---

## 定时任务

| 配置项 | 值 |
|--------|-----|
| 执行时间 | 每天 07:30 (Asia/Shanghai) |
| 任务 ID | `83b6aea6-6ed0-4fd0-866f-b857803c99f6` |
| 超时 | 600 秒 |
| 会话模式 | isolated |
| 推送渠道 | QQ (qqbot) |

---

## 报告格式

### 报告模板

模板保存在独立文件 [`template.md`](./template.md)。**生成简报前必须先读取该文件**，严格按其结构输出，版本号用 `{version}` 占位（运行时替换为当前版本）。

### 模板各字段说明

| 字段 | 要求 |
|------|------|
| **标题** | 原文英文，不翻译。Nature系文章直接用邮件标题；arXiv/bioRxiv用API返回的英文标题 |
| **作者** | 第一作者 + 通讯作者（或 et al.）。不要把摘要片段当成作者 |
| **机构** | 通讯作者（最后一位）所属机构。不要写"From abstract" |
| **一句话概要** | 核心科学问题，用主动句，不含"本文/该研究/本工作"。≤40字 |
| **主要贡献** | 3个关键发现，用"发现/揭示/证明/提出"开头，each 1-2 句话 |
| **Critical 简评** | 5星制评分 + 3-5句专家评述。必须包含：① 历史/现状 ② Motivation ③ 突破点 ④ 局限 ⑤ future work |
| **整体趋势评述** | 2-4句，不要罗列论文标题，聚焦共同主题和领域趋势 |
| **其他发现** | 可选。无重要发现时直接省略，不要凑数 |

### 版本号规则

- 报告末尾标注：`Generated by multi-omics-briefing v{version}`
- 生成前必须读取本 SKILL.md 获取当前版本号
- 版本号三处同步更新：SKILL.md 顶栏、template.md 末尾、本地 config.json

### 必须包含

- 🏷️ **Skill 版本号**：报告末尾标注 `Generated by multi-omics-briefing v1.7.0`
- 📚 精选论文 3-5 篇
- 每篇论文：标题、作者、机构、平台/日期/DOI、链接、一句话概要、主要贡献、Critical 简评
- 📊 整体趋势评述（位于所有论文之前，用 2-4 句话点出本期主题）

### 保存路径

**必须**按以下目录结构保存，**禁止**放在 `reports/` 根目录：

```
~/Documents/bioinformatics-frontier/reports/
└── YYYY/                    # 年份 (2026)
    └── MM/                  # 月份 (04)
        └── YYYY-Wxx/        # ISO 周 (2026-W17)
            └── YYYY-MM-DD-multiomics-briefing.md
```

- ✅ `reports/2026/04/2026-W17/2026-04-25-multiomics-briefing.md`
- ❌ `reports/2026-04-25-multiomics-briefing.md`（根目录，错误）

路径计算：`YYYY` = 年份，`MM` = 月份（补零），`Wxx` = ISO 周号（`date +%V`）
如目录不存在：`mkdir -p` 创建

---

## 文件结构

```
multi-omics-briefing/
├── SKILL.md                    # Skill 定义（本文件）
├── README.md                   # 说明文档 + 更新日志
├── config.json                 # 版本与元数据
├── template.md                 # 报告模板（独立文件）
├── sent-papers.json            # 去重列表
└── scripts/
    ├── generate_briefing.py    # 主生成脚本
    └── auto-push-briefing.sh   # 自动推送脚本
```

---

## 覆盖领域

多组学数据分析 · 计算生物学 · 基因组学 · 转录组学 · 蛋白质组学 · AI for Biology · 空间转录组 · 单细胞组学 · 微生物组

## 许可证

MIT

## 作者

胖达 🐼
